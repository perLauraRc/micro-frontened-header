import { License } from "../constants/license";
import { colors } from "../constants/styled-variables";

export const resolveGridBuild = (agileEstimation: License) => {
  console.log(agileEstimation);
  return colors.green;
};
